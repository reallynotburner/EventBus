{
    "name": "EventBus",
    "description": "Events without using the event loop, organizes callbacks via attach/detach/emit",
    "version": "0.0.1",
    "dependencies": {},
    "devDependencies": {
        "jest": "^22.4.4"
    },
    "scripts": {
        "test": "jest"
    }
}
